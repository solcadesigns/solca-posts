{
  "name": "solca",
  "version": "0.1.0",
  "description": "Solca · Ciencia y Consultoría — sitio principal",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build && node scripts/patch-cron-handler.mjs",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run build && wrangler deploy && node scripts/verify-live.mjs",
    "verify": "node scripts/verify-live.mjs"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.9",
    "@astrojs/cloudflare": "^12.0.0",
    "@astrojs/rss": "^4.0.0",
    "@astrojs/sitemap": "^3.0.0",
    "astro": "^5.0.0",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
